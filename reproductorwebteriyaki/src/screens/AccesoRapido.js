import { Form, FormLabel } from "react-bootstrap";
//import { Card, CardContent } from "@/components/ui/card"

function AccesoRapidoScreen(){
   // const AlbumCard = ({ title, artist, image}) => (
//        <Card className="album-card">
//            <CardContent>
//                <img src={image} alt={title} className="album-image" />
//                <FormLabel className="album-title">{title}</FormLabel>
//                <FormLabel className="album-artist">{artist}</FormLabel>
//            </CardContent>
//        </Card>
  //  )

    return(
        <main className="acceso-rapido-container">
            <FormLabel className="recomendacion-label">Para ti</FormLabel>
        </main>
    )
}

export default AccesoRapidoScreen;